propel:
    paths:
        schemaDir: config
        phpDir: src/Model
        migrationDir: storage/migrations
        sqlDir: storage/sql
        phpConfDir: config/propel
    database:
        connections:
            default:
                adapter: mysql
                dsn: mysql:host=localhost;dbname=odangodb
                user: 'root'
                password: ''
                settings:
                    charset: utf8
    generator:
        namespaceAutoPackage: false